import React, { Fragment } from 'react';

const Home = () => (
  <Fragment>
    <p>
      To get started, edit <code>src/App.js</code> and save to reload.
    </p>
  </Fragment>
);

export default Home;
